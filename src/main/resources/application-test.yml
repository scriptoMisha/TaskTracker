spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true

logging:
  level:
    org.hibernate.SQL: WARN
    org.springframework.security: WARN

app:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret-test-secret-32b
    access-ttl: PT15M
    refresh-ttl: P30D
    issuer: mini-tasks-test
